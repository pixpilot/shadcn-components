/**
 * The section's own wording, exported so a host can put it in whatever card or
 * heading its layout uses instead of having this package render one.
 */
export const SIGN_IN_METHODS_COPY = {
  title: 'Sign-in methods',
  description:
    'The ways you can sign in to this account. Connecting another one keeps the same account and all of its data — it only adds a new way in.',
} as const;
