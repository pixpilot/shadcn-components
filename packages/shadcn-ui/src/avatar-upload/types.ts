export interface ComponentSize {
  avatar: string;
  icon: string;
  dropZone: string;
  main: string;
}

export interface ComponentSizes {
  sm: ComponentSize;
  md: ComponentSize;
  lg: ComponentSize;
}
