import { connect } from '@formily/react';
import { Textarea as ShadcnTextarea } from '@internal/shadcn';

/**
 * Formily-connected Textarea component
 */
export const Textarea = connect(ShadcnTextarea);
